ecosystem:no_update_news = true

################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
# Run 'hyprctl monitors' to see what values to enter
monitor = eDP-1, 1920x1080@60.02000, 0x0, 1
# monitor = DP-1, 3840x2160@60, 0x0, 1
monitor = HDMI-A-1, preferred, auto, 1
monitor = HDMI-A-2, preferred, auto, 1

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$terminal = wezterm
$menu = fuzzel
$browser = vivaldi-stable
$email = thunderbird
$files = Thunar
$notes = obsidian
$statusbar = waybar 

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Make files applicating floating with a preset size
windowrulev2 = float,class:org.pulseaudio.pavucontrol
windowrulev2 = size 1200 900,class:org.pulseaudio.pavucontrol
windowrulev2 = bordersize 0,class:org.pulseaudio.pavucontrol

windowrulev2 = float,class:nm-connection-editor
windowrulev2 = size 1200 900,class:nm-connection-editor

windowrulev2 = float,class:$files
windowrulev2 = size 1200 900,class:$files
windowrulev2 = bordersize 0,class:$files

# Workspace rules (Hyprland uses window class: title format)
windowrulev2 = workspace 1, class:$terminal
windowrulev2 = workspace 2, initialTitle:"Vivaldi - Vivaldi"
windowrulev2 = workspace 3, class:$email
windowrulev2 = workspace 4, class:$notes
windowrulev2 = workspace 5, initialTitle:web.whatsapp.com_/
windowrulev2 = workspace 6, initialTitle:chatgpt.com_/
windowrulev2 = workspace 7, initialTitle:youtube.com_/
windowrulev2 = workspace 8, initialTitle:excalidraw.com_/
windowrulev2 = workspace 10, class:1Password

# Ignore maximize requests from apps. You'll probably like this.
windowrule = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrule = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0

#################
### AUTOSTART ###
#################

exec-once = nm-applet &
exec-once = $terminal &
exec-once = $statusbar &
exec-once = hyprpaper &

#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = WEZTERM_ENABLE_WAYLAND,1
env = XDG_CURRENT_DESKTOP,Hyprland
env = WAYLAND_DISPLAY,wayland-1
env = GTK_THEME,Nordic
env = GTK_ICON_THEME,Nordzy
env = GTK2_RC_FILES,"$HOME/.gtkrc-2.0"
env = XCURSOR_THEME,Nordzy-cursors

###################
### PERMISSIONS ###
###################

# See https://wiki.hyprland.org/Configuring/Permissions/
# Please note permission changes here require a Hyprland restart and are not applied on-the-fly
# for security reasons

# ecosystem {
#   enforce_permissions = 1
# }

# permission = /usr/(bin|local/bin)/grim, screencopy, allow
# permission = /usr/(lib|libexec|lib64)/xdg-desktop-portal-hyprland, screencopy, allow
# permission = /usr/(bin|local/bin)/hyprpm, plugin, allow

#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 5 
    gaps_out = 12 

    border_size = 8

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(8FBCBBcc) rgba(88C0D0cc) 45deg # Nord7 Nord8
    col.inactive_border = rgb(3B4252)  # Nord1

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true 

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle  # master 
    # font = "Inter"
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 12 
    rounding_power = 2

    # Change transparency of focused and unfocused windows
    active_opacity = 0.88
    inactive_opacity = 0.88 

    shadow {
        enabled = true 
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # See https://wiki.hyprland.org/Configuring/Animations/ for more
    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, quick 
    animation = windows, 1, 4.79, quick 
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 0, 1.94, almostLinear, fade
    animation = workspacesIn, 0, 1.21, almostLinear, fade
    animation = workspacesOut, 0, 1.94, almostLinear, fade
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = 0 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
}

# $w1 = hyprctl hyprpaper wallpaper "HDMI-A-1,~/.dotfiles/backgrounds/33214.jpg" 

#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = gb
    kb_model = pc105
    kb_variant =
    kb_options = caps:escape 
    kb_rules =
    repeat_delay = 275
    repeat_rate = 35 

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
# gestures {
#     workspace_swipe = false
# }

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = logitech-wireless-receiver-mouse
    sensitivity = 1 # -0.5
}

###################
### KEYBINDINGS ###
###################

source = $XDG_CONFIG_HOME/hypr/bindings.conf
